// Database utility file
// This will be used to initialize Prisma Client when Prisma is set up
// For now, it's a placeholder that can be extended

// When Prisma is installed, uncomment this:
// import { PrismaClient } from '@prisma/client';
// 
// const globalForPrisma = globalThis as unknown as {
//   prisma: PrismaClient | undefined;
// };
// 
// export const prisma = globalForPrisma.prisma ?? new PrismaClient();
// 
// if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;

// Temporary mock data for development
export const mockOpportunities: any[] = [];
export const mockContacts: any[] = [];
export const mockCompanies: any[] = [];

